import { ModFeature } from "isaacscript-common";

// Items change state when not looked at, can disappear or change type of item.
const QUANTUM_MOON = Isaac.GetItemIdByName("Quantum Moon");

export class QuantumMoon extends ModFeature {}
